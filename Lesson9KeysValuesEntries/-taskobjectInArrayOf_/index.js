//algoritm
//1. Get out rooms from the general Object;
//2. Get out names from the every rooms;
//3. put these all in the output array

function getPeople(obj) {
    const values = Object.values(obj)
let names = [];
for(let index = 0; index < values.length; index++){
  total.push(values[index].name);
}
return names;
}